{
  "name": "vmenus-app",
  "version": "1.0.0",
  "description": "Menú digital público. Sitio estático servido por nginx; este package.json existe solo para las pruebas.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "test": "node --test \"test/*.test.js\""
  }
}
